from selenium.webdriver.common.by import By
from pages import wait_for

import time
def test_removeitem(driver)
    assert "Home" in driver.page_source

    wait_for = driver.(CSS_selector, "a[href='login']")

